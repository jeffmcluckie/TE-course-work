package com.techelevator.farm;

public interface Singable {

    // Note that it is common to NOT specify the access modifier in an interface
    // the methods are all assumed by default to public and abstract
    String getName();
    String getSound();
}
